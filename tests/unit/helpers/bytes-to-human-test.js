import {
  bytesToHuman
} from 'lion-guardians/helpers/bytes-to-human';

module('BytesToHumanHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = bytesToHuman(42);
  ok(result);
});
