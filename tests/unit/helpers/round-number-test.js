import {
  roundNumber
} from 'lion-guardians/helpers/round-number';

module('RoundNumberHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = roundNumber(42);
  ok(result);
});
